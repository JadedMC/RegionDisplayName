name: RegionDisplayName
version: '${project.version}'
main: net.jadedmc.regiondisplayname.RegionDisplayNamePlugin
api-version: 1.13
depend:
  - WorldGuard
  - PlaceholderAPI

softdepend:
  - BetterReload

commands:
  regiondisplayname:
    description: RegionDisplayName main plugin command.
    aliases: [rdn]
    usage: /<command>
    permission: regiondisplayname.admin
